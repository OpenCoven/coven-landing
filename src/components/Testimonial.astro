---
// Trust strip — names the harnesses Coven plugs into today so visitors
// can read "this works with the CLI I already use" in one glance,
// instead of a placeholder testimonial.
const harnesses = [
  { name: 'Claude Code', vendor: 'Anthropic', logo: '/logos/anthropic.svg' },
  { name: 'Codex',       vendor: 'OpenAI',    logo: '/logos/openai.svg'    },
];
---
<div class="trust-strip" role="note" aria-label="Supported harnesses">
  <div class="trust-inner" data-reveal>
    <span class="trust-kicker">Drives the agent CLIs you already use</span>
    <div class="trust-marks">
      {harnesses.map((h) => (
        <span class="trust-mark">
          <img src={h.logo} alt="" width="18" height="18" />
          <span class="trust-mark-name">{h.name}</span>
          <span class="trust-mark-vendor">· {h.vendor}</span>
        </span>
      ))}
      <span class="trust-mark trust-mark--future">
        <span class="trust-mark-name">+ more</span>
        <span class="trust-mark-vendor">adapter-ready</span>
      </span>
    </div>
  </div>
</div>

<style>
  .trust-strip {
    position: relative;
    z-index: 1;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    background: rgba(14, 11, 22, 0.6);
    padding: 22px 32px;
  }
  .trust-inner {
    max-width: 1280px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 28px;
    flex-wrap: wrap;
  }
  .trust-kicker {
    font-family: var(--mono);
    font-size: 0.625rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(201, 167, 255, 0.75);
  }
  .trust-marks {
    display: flex;
    align-items: center;
    gap: 18px;
    flex-wrap: wrap;
  }
  .trust-mark {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 10px;
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.02);
  }
  .trust-mark img {
    display: block;
    border-radius: 3px;
    background: #fff;
    padding: 2px;
    box-sizing: content-box;
  }
  .trust-mark-name {
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--white);
  }
  .trust-mark-vendor {
    font-family: var(--mono);
    font-size: 0.625rem;
    color: rgba(201, 167, 255, 0.6);
    letter-spacing: 0.04em;
  }
  .trust-mark--future {
    border-style: dashed;
    background: transparent;
  }
  .trust-mark--future .trust-mark-name {
    color: rgba(201, 167, 255, 0.78);
  }

  /* Light theme: the strip becomes a quiet light divider. :global() wraps
     the <html> selector so the scoped .trust-* classes still match. */
  :global(html[data-theme="light"]) .trust-strip { background: rgba(255, 255, 255, 0.5); }
  :global(html[data-theme="light"]) .trust-kicker { color: #6E5FB0; }
  :global(html[data-theme="light"]) .trust-mark {
    border-color: rgba(122, 111, 179, 0.18);
    background: rgba(122, 111, 179, 0.05);
  }
  :global(html[data-theme="light"]) .trust-mark-name { color: #1B1726; }
  :global(html[data-theme="light"]) .trust-mark-vendor { color: rgba(90, 76, 158, 0.78); }
  :global(html[data-theme="light"]) .trust-mark--future .trust-mark-name { color: #6E5FB0; }

  @media (max-width: 720px) {
    .trust-strip { padding: 18px 20px; }
    .trust-inner { gap: 18px; }
    .trust-kicker { font-size: 0.5625rem; }
  }
  @media (max-width: 520px) {
    .trust-inner { flex-direction: column; gap: 12px; }
    .trust-marks { gap: 8px; }
  }
</style>
