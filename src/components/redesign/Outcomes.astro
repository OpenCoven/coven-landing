<section id="outcomes" style="border-top: 1px solid var(--cv-border-hairline); background: var(--cv-bg-panel)">
  <div style="max-width: 1140px; margin: 0 auto; padding: 84px 32px 88px">
    <h2 data-anim="" style="font-family: 'EB Garamond', serif; font-weight: 400; letter-spacing: -0.018em; margin: 0; font-size: 42px; margin-bottom: 38px">What changes on day one</h2>
    <div data-anim-group="" style="display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 20px">
      <div style="display: flex; flex-direction: column; gap: 10px; background: var(--cv-bg-base); border: 1px solid var(--cv-border-hairline); border-radius: 14px; padding: 24px 26px">
        <span style="font-family: 'JetBrains Mono', monospace; font-variant-ligatures: none; font-size: 10px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--cv-text-muted)">01</span>
        <h3 style="font-size: 19px; font-weight: 550; letter-spacing: -0.01em; line-height: 1.25; margin: 0; max-width: 22ch">Agents stop overwriting each other</h3>
        <p style="font-size: 14.5px; line-height: 1.65; color: var(--cv-text-secondary); margin: 0; max-width: 34ch; text-wrap: pretty">A claim on a surface holds until the work lands or you release it. Coven tells the second session before it writes.</p>
      </div>
      <div style="display: flex; flex-direction: column; gap: 10px; background: var(--cv-bg-base); border: 1px solid var(--cv-border-hairline); border-radius: 14px; padding: 24px 26px">
        <span style="font-family: 'JetBrains Mono', monospace; font-variant-ligatures: none; font-size: 10px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--cv-text-muted)">02</span>
        <h3 style="font-size: 19px; font-weight: 550; letter-spacing: -0.01em; line-height: 1.25; margin: 0; max-width: 22ch">Claims and memory outlive the session</h3>
        <p style="font-size: 14.5px; line-height: 1.65; color: var(--cv-text-secondary); margin: 0; max-width: 34ch; text-wrap: pretty">Close the terminal, restart the machine. The claims, the events, and what each familiar learned are still there.</p>
      </div>
      <div style="display: flex; flex-direction: column; gap: 10px; background: var(--cv-bg-base); border: 1px solid var(--cv-border-hairline); border-radius: 14px; padding: 24px 26px">
        <span style="font-family: 'JetBrains Mono', monospace; font-variant-ligatures: none; font-size: 10px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--cv-text-muted)">03</span>
        <h3 style="font-size: 19px; font-weight: 550; letter-spacing: -0.01em; line-height: 1.25; margin: 0; max-width: 22ch">You stay the authority</h3>
        <p style="font-size: 14.5px; line-height: 1.65; color: var(--cv-text-secondary); margin: 0; max-width: 34ch; text-wrap: pretty">Guards stop anything past their line and wait for your yes, with the diff in front of you.</p>
      </div>
    </div>
    <a href="/how-it-works" style="display: inline-flex; align-items: center; gap: 8px; margin-top: 36px; padding: 12px 18px; border: 1px solid color-mix(in oklch, var(--cv-accent) 32%, transparent); border-radius: 8px; font-size: 15px; font-weight: 500; color: var(--cv-accent); transition: background-color 160ms cubic-bezier(0.2,0,0,1), border-color 160ms cubic-bezier(0.2,0,0,1)" class="rh3">How the runtime does this <span style="font-size: 13px" aria-hidden="true">→</span></a>
  </div>
</section>

<!-- Final CTA -->
